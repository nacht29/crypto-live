# AWS region for SDK clients
AWS_REGION=ap-southeast-1

# DynamoDB table name storing miniTicker snapshots
DYNAMO_TABLE_NAME=crypto-live-miniticker

# Comma-separated stream keys (e.g. btcusdt@miniTicker,ethusdt@miniTicker)
UI_STREAMS=

# Credentials are sourced from environment / IAM role in Vercel
